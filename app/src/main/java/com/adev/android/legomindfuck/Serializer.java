package com.adev.android.legomindfuck;

public class Serializer {
    //String base = "#" + APP + EXEC + BASE + CLOCKWISE + (qt+cod_qt) + (speed + cod_speed) + "#";

    /*
    *
    * char APP = 'a';

        char EXEC = 'e';

        int cod_speed = 110;
        int speed = 10;
        int cod_qt = 10;
        int qt = 5;

        int BASE = 1;
        int BRACCIO = 2;
        int SOLLEVATORE = 3;

        char CLOCKWISE = '+', BACKWARD = '+', DOWN = '+';
        char ANTICLOCKWISE = '-', FORWARD ='-', UP = '-';
    *
    * */
}
